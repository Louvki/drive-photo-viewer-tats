{
    "name": "tattoo-portfolio-generator",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "nuxt dev",
        "build": "nuxt build",
        "generate": "nuxt generate",
        "preview": "nuxt preview",
        "deploy": "nuxt generate && cd .output/public && git init && git add -A && git commit -m 'Deploy' && git push -f $DEPLOY_GIT_REPO main && cd ../.."
    },
    "dependencies": {
        "googleapis": "^133.0.0"
    },
    "devDependencies": {
        "@types/node": "^20.12.12",
        "nuxt": "^3.13.2",
        "typescript": "^5.6.3"
    }
}